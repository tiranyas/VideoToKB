---
phase: 11-full-qa-suite-vitest-expansion-and-playwright-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__tests__/youtube-resolver.test.ts
  - src/lib/__tests__/gdrive-resolver.test.ts
autonomous: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "YouTube resolver has tests for ID extraction from all URL formats (watch, short, embed, shorts)"
    - "YouTube resolver has tests for the fallback flow (InnerTube fails -> Supadata -> error)"
    - "Google Drive resolver has tests for file ID extraction from both URL patterns"
    - "Google Drive resolver has tests for download URL resolution with fallback candidates"
  artifacts:
    - path: "src/lib/__tests__/youtube-resolver.test.ts"
      provides: "YouTube resolver unit tests"
      min_lines: 80
    - path: "src/lib/__tests__/gdrive-resolver.test.ts"
      provides: "Google Drive resolver unit tests"
      min_lines: 60
  key_links:
    - from: "src/lib/__tests__/youtube-resolver.test.ts"
      to: "src/lib/youtube-resolver.ts"
      via: "import { extractYouTubeId, isYouTubeUrl, getYouTubeTranscript }"
      pattern: "import.*youtube-resolver"
    - from: "src/lib/__tests__/gdrive-resolver.test.ts"
      to: "src/lib/gdrive-resolver.ts"
      via: "import { resolveGoogleDriveUrl }"
      pattern: "import.*gdrive-resolver"
---

<objective>
Add comprehensive Vitest unit tests for the YouTube and Google Drive video resolvers.

Purpose: These resolvers contain complex URL parsing and multi-strategy fallback logic that is currently untested. Covers requirements TEST-03 and TEST-04.
Output: Two new test files with full coverage of pure functions and mocked-fetch integration tests.
</objective>

<execution_context>
@C:/Users/tiran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tiran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-qa-suite-vitest-expansion-and-playwright-e2e-tests/11-RESEARCH.md

@src/lib/youtube-resolver.ts
@src/lib/gdrive-resolver.ts
@src/lib/__tests__/loom-resolver.test.ts
</context>

<interfaces>
<!-- Existing test pattern from loom-resolver.test.ts -->
```typescript
// Standard test setup pattern used across all resolver tests:
const mockFetch = vi.fn();
vi.stubGlobal('fetch', mockFetch);
beforeEach(() => { mockFetch.mockReset(); });
```

<!-- YouTube resolver exports -->
```typescript
export function extractYouTubeId(url: string): string | null;
export function isYouTubeUrl(url: string): boolean;
export class YouTubeExtractionError extends Error {
  isServerBlocked: boolean;
}
export async function getYouTubeTranscript(url: string): Promise<YouTubeTranscriptResult>;
```

<!-- Google Drive resolver exports -->
```typescript
export async function resolveGoogleDriveUrl(shareUrl: string): Promise<VideoInfo>;
// VideoInfo = { videoUrl: string; title: string }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: YouTube resolver tests</name>
  <files>src/lib/__tests__/youtube-resolver.test.ts</files>
  <behavior>
    - extractYouTubeId: extracts ID from watch URL, short URL, embed URL, shorts URL
    - extractYouTubeId: returns null for non-YouTube URL, malformed URLs, empty string
    - isYouTubeUrl: returns true for valid YouTube URLs, false for non-YouTube
    - getYouTubeTranscript: throws YouTubeExtractionError with isServerBlocked=false for invalid URL
    - getYouTubeTranscript: returns transcript when InnerTube succeeds (mock fetch to return caption data with events/segs)
    - getYouTubeTranscript: falls back to Supadata when InnerTube fails (mock Supadata module)
    - getYouTubeTranscript: throws YouTubeExtractionError with isServerBlocked=true when both methods fail
    - getYouTubeTranscript: fetches title via oEmbed (mock fetch for oEmbed response)
  </behavior>
  <action>
    Create `src/lib/__tests__/youtube-resolver.test.ts` following the established pattern from loom-resolver.test.ts.

    Structure into three describe blocks:
    1. `describe('extractYouTubeId')` — pure function tests using `it.each` for all URL formats (watch?v=, youtu.be/, embed/, shorts/). Test null returns for non-YouTube URLs.
    2. `describe('isYouTubeUrl')` — simple boolean wrapper tests.
    3. `describe('getYouTubeTranscript')` — mock `fetch` globally with `vi.stubGlobal`. Mock `@supadata/js` with `vi.mock`. Test the 3-tier fallback: InnerTube success, InnerTube fail + Supadata success, both fail.

    For InnerTube mock responses, return a JSON structure with `captions.playerCaptionsTracklistRenderer.captionTracks[0].baseUrl` pointing to a mock URL, then mock the caption fetch to return `{ events: [{ tStartMs: 0, dDurationMs: 1000, segs: [{ utf8: "Hello" }] }] }`.

    For Supadata mock, use `vi.mock('@supadata/js', ...)` returning a mock `Supadata` class whose `youtube.transcript()` returns `{ content: [{ text: "Hello", offset: 0, duration: 1000 }] }`.

    Mock oEmbed fetch to return `{ title: "Test Video" }`.

    Use `vi.stubEnv('SUPADATA_API_KEY', 'test-key')` where needed.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/youtube-resolver.test.ts -x</automated>
  </verify>
  <done>All YouTube resolver tests pass. Coverage includes: ID extraction from 4 URL formats, null for invalid URLs, isYouTubeUrl boolean, getYouTubeTranscript with InnerTube success, Supadata fallback, and both-fail error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Google Drive resolver tests</name>
  <files>src/lib/__tests__/gdrive-resolver.test.ts</files>
  <behavior>
    - resolveGoogleDriveUrl: throws for invalid Google Drive URL (not matching expected patterns)
    - resolveGoogleDriveUrl: extracts file ID from /file/d/{id}/view format
    - resolveGoogleDriveUrl: extracts file ID from /open?id={id} format
    - resolveGoogleDriveUrl: returns videoUrl when first download candidate returns non-HTML content
    - resolveGoogleDriveUrl: tries next candidate when first returns HTML
    - resolveGoogleDriveUrl: extracts download URL from HTML form action when all candidates return HTML
    - resolveGoogleDriveUrl: throws when no download URL can be resolved (all candidates fail)
    - resolveGoogleDriveUrl: extracts title from content-disposition header
    - resolveGoogleDriveUrl: uses default title when content-disposition is missing
  </behavior>
  <action>
    Create `src/lib/__tests__/gdrive-resolver.test.ts` following the loom-resolver.test.ts pattern.

    Structure into two describe blocks:
    1. `describe('URL parsing')` — test file ID extraction from both URL patterns (/file/d/{id}/view and /open?id={id}). Test rejection of invalid URLs.
    2. `describe('resolveGoogleDriveUrl')` — mock global fetch.

    For the download resolution tests, mock fetch to simulate the 3-candidate fallback:
    - Success case: First candidate returns non-HTML content-type (e.g., `video/mp4`) -> returns that URL.
    - Fallback case: First candidate returns `text/html`, second returns `video/mp4` -> returns second URL.
    - HTML form extraction case: All candidates return HTML, but the HTML contains `action="https://drive.usercontent.google.com/download..."` -> extracts and returns that URL.
    - Failure case: All candidates fail (return 404) -> throws descriptive error.

    For title extraction, mock a HEAD request returning `content-disposition: attachment; filename="My Video.mp4"` -> title should be "My Video" (extension stripped).

    Note: `extractFileId` and `resolveDirectDownloadUrl` are not exported. Test them indirectly through `resolveGoogleDriveUrl`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/gdrive-resolver.test.ts -x</automated>
  </verify>
  <done>All Google Drive resolver tests pass. Coverage includes: file ID extraction from both URL patterns, download URL resolution with candidate fallback, HTML form action extraction, title extraction from content-disposition, and proper error messages.</done>
</task>

</tasks>

<verification>
npx vitest run src/lib/__tests__/youtube-resolver.test.ts src/lib/__tests__/gdrive-resolver.test.ts -x
</verification>

<success_criteria>
- YouTube resolver has 8+ tests covering pure functions and mocked integration
- Google Drive resolver has 7+ tests covering URL parsing and download resolution
- All existing tests still pass (`npx vitest run`)
- Tests follow established project patterns (vi.stubGlobal, vi.mock, describe/it)
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-qa-suite-vitest-expansion-and-playwright-e2e-tests/11-01-SUMMARY.md`
</output>
