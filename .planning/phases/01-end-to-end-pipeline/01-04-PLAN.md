---
phase: 01-end-to-end-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - vercel.json
autonomous: false
requirements:
  - PRUX-02

must_haves:
  truths:
    - "The application is deployed and accessible on a Vercel URL"
    - "The full pipeline works on Vercel: paste Loom URL -> see progress -> view article"
    - "SSE progress events stream in real time on Vercel (not buffered)"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel configuration (if needed for region/function settings)"
  key_links:
    - from: "Vercel deployment"
      to: "src/app/api/process/route.ts"
      via: "Vercel serverless function with 300s Fluid Compute timeout"
      pattern: "maxDuration.*300"
---

<objective>
Deploy the application to Vercel and verify the entire pipeline works end-to-end on a real deployment, not just localhost.

Purpose: Phase 1 success criteria explicitly requires Vercel deployment. SSE streaming and long-running functions behave differently on Vercel vs dev server -- this must be validated.
Output: Working Vercel deployment URL with verified end-to-end pipeline.
</objective>

<execution_context>
@C:/Users/tiran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tiran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-end-to-end-pipeline/01-RESEARCH.md
@.planning/phases/01-end-to-end-pipeline/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel with environment variables</name>
  <files>vercel.json</files>
  <action>
1. Create `vercel.json` if any configuration is needed (region selection, function config). If default Next.js config suffices, create a minimal one:
   ```json
   {
     "$schema": "https://openapi.vercel.sh/vercel.json"
   }
   ```

2. Deploy using Vercel CLI:
   - Run `npx vercel --yes` to create project and deploy (will prompt for Vercel login if needed -- this is the one acceptable auth checkpoint)
   - Set environment variables on Vercel:
     - `npx vercel env add ASSEMBLYAI_API_KEY production` (user provides value)
     - `npx vercel env add ANTHROPIC_API_KEY production` (user provides value)
   - Redeploy after setting env vars: `npx vercel --prod`

3. Record the deployment URL for verification.

Note: If the Vercel CLI requires authentication, this task will pause for the user to log in via browser. This is expected.
  </action>
  <verify>
    <automated>cd C:/Users/tiran/OneDrive/Desktop/Tiran/Obsidian/TIran/VideoToKB && npx vercel ls 2>&1 | head -10</automated>
  </verify>
  <done>Application deployed to Vercel. Deployment URL is accessible. Environment variables are set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end pipeline on Vercel deployment</name>
  <files>none</files>
  <action>Human verifies the deployed application works end-to-end with a real Loom video. See how-to-verify for test steps.</action>
  <what-built>Complete end-to-end pipeline deployed on Vercel: Loom URL input -> real-time progress -> generated KB article</what-built>
  <how-to-verify>
    1. Open the Vercel deployment URL in your browser
    2. Confirm the homepage shows: centered URL input, template dropdown (How-to Guide selected), "Create Article" button, no navigation/login/dashboard
    3. Paste a real Loom share URL (e.g., a short public Loom recording)
    4. Click "Create Article"
    5. Watch the progress display -- confirm steps update in real time:
       - "Resolving video URL" should show spinning, then checkmark
       - "Transcribing audio" should show spinning, then checkmark (this takes 30-90 seconds)
       - "Generating article" should show spinning, then checkmark
       - "Complete" should show checkmark
    6. Confirm the generated article appears in an editable text area
    7. Try editing the article text -- confirm it is editable
    8. Click "Start Over" -- confirm it returns to the input form
    9. Try submitting an invalid URL (e.g., "not-a-url") -- confirm an error message appears
    10. Try submitting a Loom URL that doesn't exist -- confirm a specific error message appears

    Expected: All 10 steps pass. If SSE events arrive all at once instead of streaming, report that specifically.
  </how-to-verify>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>User has approved the Vercel deployment. Pipeline works end-to-end with real Loom videos.</done>
  <resume-signal>Type "approved" if all steps pass, or describe any issues you encountered</resume-signal>
</task>

</tasks>

<verification>
- Vercel deployment is live and accessible
- Full pipeline runs successfully with a real Loom video
- SSE events stream in real time (not buffered)
- Error messages are specific and actionable
- UI matches requirements: centered input, template dropdown, no nav
</verification>

<success_criteria>
- Application deployed on Vercel and accessible via public URL
- User can paste a Loom URL, see real-time progress, and view a generated KB article
- Errors produce specific messages (not generic "something went wrong")
- The entire flow works end-to-end on Vercel deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-end-to-end-pipeline/01-04-SUMMARY.md`
</output>
