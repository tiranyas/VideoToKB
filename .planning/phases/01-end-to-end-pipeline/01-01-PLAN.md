---
phase: 01-end-to-end-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/types/index.ts
  - src/utils/cn.ts
  - src/lib/templates/how-to.ts
  - .env.local.example
  - .gitignore
  - vitest.config.ts
  - src/lib/__tests__/loom-resolver.test.ts
  - src/lib/__tests__/transcription.test.ts
  - src/lib/__tests__/article-generator.test.ts
  - src/lib/__tests__/pipeline.test.ts
autonomous: true
requirements:
  - UILP-02
  - GENR-02

must_haves:
  truths:
    - "Next.js 16 project builds and runs with `npm run dev`"
    - "All shared types are defined and exported from src/types/index.ts"
    - "Test infrastructure runs with `npx vitest run` (all tests fail as expected -- RED state)"
    - "How-to template prompt exists as a reusable constant"
  artifacts:
    - path: "src/types/index.ts"
      provides: "PipelineStep, StepStatus, ProgressEvent, PipelineResult, LoomVideoInfo types"
      exports: ["PipelineStep", "StepStatus", "ProgressEvent", "PipelineResult", "LoomVideoInfo"]
    - path: "src/utils/cn.ts"
      provides: "cn() utility for Tailwind class merging"
      exports: ["cn"]
    - path: "src/lib/templates/how-to.ts"
      provides: "How-to Guide template system prompt"
      exports: ["HOW_TO_TEMPLATE"]
    - path: "vitest.config.ts"
      provides: "Vitest configuration for Next.js"
    - path: "src/lib/__tests__/loom-resolver.test.ts"
      provides: "Loom resolver test scaffolds"
    - path: "src/lib/__tests__/transcription.test.ts"
      provides: "Transcription + preprocessing test scaffolds"
    - path: "src/lib/__tests__/article-generator.test.ts"
      provides: "Article generator test scaffolds"
    - path: "src/lib/__tests__/pipeline.test.ts"
      provides: "Pipeline orchestrator test scaffolds"
  key_links:
    - from: "vitest.config.ts"
      to: "src/lib/__tests__/*.test.ts"
      via: "Vitest test runner discovers test files"
      pattern: "include.*src"
    - from: "src/types/index.ts"
      to: "src/lib/__tests__/*.test.ts"
      via: "Tests import shared types"
      pattern: "import.*from.*types"
---

<objective>
Scaffold the Next.js 16 project, define all shared types/contracts, create the How-to template prompt, and set up the test infrastructure with failing test scaffolds (Wave 0).

Purpose: Establish the foundation that all subsequent plans build on. Types define contracts between pipeline stages. Test scaffolds enable TDD-style verification as services are implemented.
Output: Buildable Next.js project with types, template, and test infrastructure ready.
</objective>

<execution_context>
@C:/Users/tiran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tiran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-end-to-end-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with dependencies and shared types</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/types/index.ts, src/utils/cn.ts, src/lib/templates/how-to.ts, .env.local.example, .gitignore</files>
  <action>
1. Create the Next.js 16 project using `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --yes` (use current directory since repo exists).

2. Install production dependencies: `npm install assemblyai @anthropic-ai/sdk zod clsx tailwind-merge sonner lucide-react`

3. Install dev dependencies: `npm install -D vitest @vitejs/plugin-react @tailwindcss/typography`

4. Create `src/types/index.ts` with these exact type definitions:
   ```typescript
   export type PipelineStep = 'resolve' | 'transcribe' | 'generate' | 'done';
   export type StepStatus = 'pending' | 'in_progress' | 'complete' | 'error';

   export interface ProgressEvent {
     step: PipelineStep | 'error';
     status: StepStatus;
     message?: string;
     article?: string;
   }

   export interface LoomVideoInfo {
     videoUrl: string;
     title: string;
   }

   export interface TranscriptResult {
     text: string;
     paragraphs: { text: string; start: number; end: number }[];
     wordCount: number;
   }

   export interface PipelineResult {
     article: string;
     title: string;
   }
   ```

5. Create `src/utils/cn.ts`:
   ```typescript
   import { clsx, type ClassValue } from 'clsx';
   import { twMerge } from 'tailwind-merge';
   export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
   ```

6. Create `src/lib/templates/how-to.ts` with a detailed system prompt for generating How-to Guide KB articles. The prompt should instruct Claude to:
   - Generate a structured How-to article with clear steps
   - Include: title (as H1), brief overview, prerequisites (if any), numbered steps with explanations, tips/notes boxes, and a summary
   - Output in Markdown format
   - Base the article on the provided video transcript
   - Keep language clear and professional
   - Export as `HOW_TO_TEMPLATE` constant string

7. Create `.env.local.example` with placeholder keys:
   ```
   ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here
   ANTHROPIC_API_KEY=your_anthropic_api_key_here
   ```

8. Update `.gitignore` to include `.env.local` if not already present.

9. Replace the default `src/app/page.tsx` with a minimal placeholder that just renders "VideoToKB" centered on the page (will be replaced in Plan 03).

10. Ensure `src/app/layout.tsx` has no navigation elements -- just the basic html/body wrapper with Toaster from sonner. This satisfies UILP-02 (no login, no dashboard, no navigation).
  </action>
  <verify>
    <automated>cd C:/Users/tiran/OneDrive/Desktop/Tiran/Obsidian/TIran/VideoToKB && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js project builds successfully. All types exported from src/types/index.ts. cn() utility exists. How-to template prompt exists. .env.local.example documents required keys. Layout has no navigation (UILP-02).</done>
</task>

<task type="auto">
  <name>Task 2: Create test infrastructure with failing test scaffolds (Wave 0)</name>
  <files>vitest.config.ts, src/lib/__tests__/loom-resolver.test.ts, src/lib/__tests__/transcription.test.ts, src/lib/__tests__/article-generator.test.ts, src/lib/__tests__/pipeline.test.ts</files>
  <action>
1. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import react from '@vitejs/plugin-react';
   import path from 'path';

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'node',
       include: ['src/**/*.test.ts'],
       globals: true,
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   });
   ```

2. Create `src/lib/__tests__/loom-resolver.test.ts` with tests that import from `@/lib/loom-resolver` (file does not exist yet -- tests will fail):
   - Test: valid Loom share URL (https://www.loom.com/share/{32-hex-chars}) is accepted
   - Test: invalid URL format throws descriptive error
   - Test: URL with wrong domain throws error
   - Test: extracts video ID correctly from URL
   - Test: handles 404 response with "video not found" error
   - Test: handles 403 response with "video is private" error
   - Mock `fetch` for all tests (do not make real HTTP calls)

3. Create `src/lib/__tests__/transcription.test.ts` with tests importing from `@/lib/transcription`:
   - Test: `preprocessTranscript` removes filler words (um, uh, like, you know, I mean, basically, actually, literally, right)
   - Test: `preprocessTranscript` collapses multiple spaces after removal
   - Test: `preprocessTranscript` formats timestamps as M:SS
   - Test: `preprocessTranscript` filters out near-empty paragraphs (< 10 chars after cleaning)
   - Test: `preprocessTranscript` preserves substantive content
   - Test: `transcribeVideo` calls AssemblyAI with correct params (mock AssemblyAI SDK)
   - Test: `transcribeVideo` throws on transcription error status

4. Create `src/lib/__tests__/article-generator.test.ts` with tests importing from `@/lib/article-generator`:
   - Test: `generateArticle` calls Anthropic with claude-sonnet-4-5 model
   - Test: `generateArticle` passes template as system prompt
   - Test: `generateArticle` includes transcript in user message
   - Test: `generateArticle` returns text content from response
   - Test: `generateArticle` throws when no text block in response
   - Mock Anthropic SDK for all tests

5. Create `src/lib/__tests__/pipeline.test.ts` with tests importing from `@/lib/pipeline`:
   - Test: pipeline emits progress events in order: resolve -> transcribe -> generate -> done
   - Test: each step emits in_progress then complete status
   - Test: resolve failure emits error with "Loom" in message
   - Test: transcribe failure emits error with "transcription" in message
   - Test: generate failure emits error with "generation" in message
   - Test: done event includes the generated article text
   - Mock all service modules (loom-resolver, transcription, article-generator)

All tests should be written to import from their respective modules and call real function signatures. They WILL fail because the implementation files do not exist yet. This is the expected RED state for Wave 0.
  </action>
  <verify>
    <automated>cd C:/Users/tiran/OneDrive/Desktop/Tiran/Obsidian/TIran/VideoToKB && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest runs and discovers all 4 test files. All tests fail (RED state) because implementation modules do not exist. Test count is 20+ across 4 files. No test passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx vitest run` discovers all test files and reports failures (RED state)
- `src/types/index.ts` exports PipelineStep, StepStatus, ProgressEvent, LoomVideoInfo, TranscriptResult, PipelineResult
- `.env.local.example` exists with ASSEMBLYAI_API_KEY and ANTHROPIC_API_KEY placeholders
</verification>

<success_criteria>
- Next.js 16 project builds and `npm run dev` starts the dev server
- All shared types defined and exported
- How-to template prompt ready for use by article generator
- Vitest configured and running, all test scaffolds in RED state
- No navigation/login/dashboard in layout (UILP-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-end-to-end-pipeline/01-01-SUMMARY.md`
</output>
