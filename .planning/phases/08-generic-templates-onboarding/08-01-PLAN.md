---
phase: 08-generic-templates-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/templates/agent4-html.ts
  - src/lib/branding.ts
  - src/lib/__tests__/branding.test.ts
  - src/lib/__tests__/template-neutralization.test.ts
autonomous: true
requirements:
  - SC-5
  - SC-6
  - SC-7

must_haves:
  truths:
    - "No default template contains #6d28d9 (FinBot purple) anywhere"
    - "All built-in templates use {{placeholder}} variables for colors and font"
    - "replacePlaceholders() fills branding values when provided"
    - "replacePlaceholders() uses neutral defaults (#2563eb) when no branding provided"
    - "A user who skips onboarding gets articles with neutral blue, not purple"
  artifacts:
    - path: "src/lib/branding.ts"
      provides: "replacePlaceholders utility and BRANDING_DEFAULTS constant"
      exports: ["replacePlaceholders", "BRANDING_DEFAULTS"]
    - path: "src/lib/templates/agent4-html.ts"
      provides: "Neutralized templates with {{placeholder}} variables, updated buildHtmlPrompt with branding param"
      exports: ["buildHtmlPrompt", "DEFAULT_PLATFORM_PROFILES"]
    - path: "src/lib/__tests__/branding.test.ts"
      provides: "Unit tests for replacePlaceholders"
    - path: "src/lib/__tests__/template-neutralization.test.ts"
      provides: "Unit tests verifying no #6d28d9 in any default template"
  key_links:
    - from: "src/lib/templates/agent4-html.ts"
      to: "src/lib/branding.ts"
      via: "import replacePlaceholders"
      pattern: "import.*replacePlaceholders.*from.*branding"
    - from: "src/lib/templates/agent4-html.ts buildHtmlPrompt"
      to: "src/lib/pipeline.ts runPhaseB"
      via: "buildHtmlPrompt now accepts optional branding 3rd param"
      pattern: "buildHtmlPrompt\\(.*branding"
---

<objective>
Neutralize all built-in templates from FinBot purple to neutral blue and add {{placeholder}} variable replacement so workspace branding flows into generated HTML.

Purpose: This is the foundation for branded article generation. Every template must use placeholders instead of hardcoded colors, and a utility must exist to replace them with workspace branding or neutral defaults.

Output: Updated `agent4-html.ts` with neutralized templates + `{{placeholder}}` variables, new `branding.ts` utility, updated `buildHtmlPrompt` signature, and comprehensive tests.
</objective>

<execution_context>
@C:/Users/tiran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tiran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-generic-templates-onboarding/08-CONTEXT.md
@.planning/phases/08-generic-templates-onboarding/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/index.ts:
```typescript
export interface WorkspaceBranding {
  primaryColor?: string;
  secondaryColor?: string;
  accentColor?: string;
  logoUrl?: string;
  fontFamily?: string;
  customCss?: string;
}

export interface PlatformProfile {
  id: string;
  name: string;
  htmlPrompt: string;
  htmlTemplate: string;
  isDefault?: boolean;
}
```

From src/lib/templates/agent4-html.ts (current signature):
```typescript
export function buildHtmlPrompt(htmlPrompt: string, htmlTemplate: string): string;
export const DEFAULT_PLATFORM_PROFILES: PlatformProfile[];
```

From src/lib/pipeline.ts (PhaseBInput already has branding):
```typescript
export interface PhaseBInput {
  article: string;
  htmlPrompt: string;
  htmlTemplate: string;
  branding?: {
    primaryColor?: string;
    secondaryColor?: string;
    accentColor?: string;
    fontFamily?: string;
    logoUrl?: string;
    customCss?: string;
  };
  // ...
}
```
Note: pipeline.ts currently injects branding via prompt text (lines 166-173). After this plan, it should ALSO pass branding through buildHtmlPrompt for template placeholder replacement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create branding utility and tests</name>
  <files>src/lib/branding.ts, src/lib/__tests__/branding.test.ts</files>
  <behavior>
    - replacePlaceholders("color: {{primaryColor}}", { primaryColor: "#ff0000" }) returns "color: #ff0000"
    - replacePlaceholders("color: {{primaryColor}}", undefined) returns "color: #2563eb" (default)
    - replacePlaceholders("{{primaryColor}} {{accentColor}}", { primaryColor: "#ff0000" }) returns "#ff0000 #2563eb" (partial branding uses defaults for missing)
    - replacePlaceholders("no placeholders here", {}) returns "no placeholders here" (passthrough)
    - replacePlaceholders("{{unknownVar}}", {}) returns "{{unknownVar}}" (unknown vars left as-is)
    - BRANDING_DEFAULTS.primaryColor is "#2563eb"
    - BRANDING_DEFAULTS.accentColor is "#2563eb"
    - BRANDING_DEFAULTS.fontFamily is "system-ui, -apple-system, sans-serif"
  </behavior>
  <action>
    1. Create `src/lib/branding.ts`:
       - Export `BRANDING_DEFAULTS` record mapping placeholder names to default values: primaryColor="#2563eb", accentColor="#2563eb", secondaryColor="#e5e7eb", fontFamily="system-ui, -apple-system, sans-serif", textColor="#1f2937", mutedColor="#6b7280"
       - Export `replacePlaceholders(template: string, branding: WorkspaceBranding | undefined): string` that:
         - Builds a merged values object: BRANDING_DEFAULTS overridden by non-empty branding fields
         - Uses `template.replace(/\{\{(\w+)\}\}/g, (_, key) => values[key] ?? _)` to replace
       - Import `WorkspaceBranding` from `@/types`

    2. Create `src/lib/__tests__/branding.test.ts` with tests for all behaviors above. Use vitest `describe`/`it` pattern.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/branding.test.ts -x</automated>
  </verify>
  <done>replacePlaceholders works with full branding, partial branding, no branding, and unknown variables. BRANDING_DEFAULTS exported with correct values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Neutralize templates and update buildHtmlPrompt</name>
  <files>src/lib/templates/agent4-html.ts, src/lib/__tests__/template-neutralization.test.ts, src/lib/pipeline.ts</files>
  <behavior>
    - No DEFAULT_PLATFORM_PROFILES entry contains the string "#6d28d9" anywhere in htmlPrompt or htmlTemplate
    - GENERIC_TEMPLATE contains "{{primaryColor}}" not a hardcoded hex color for --kb-primary
    - GENERIC_TEMPLATE contains "{{accentColor}}" not a hardcoded hex color for --kb-accent
    - GENERIC_TEMPLATE contains "{{fontFamily}}" for --kb-font
    - HELPJUICE_TEMPLATE uses "{{primaryColor}}" where it previously used "#6d28d9"
    - HELPJUICE_PROMPT mentions "{{primaryColor}}" not "#6d28d9"
    - buildHtmlPrompt(prompt, template, branding) calls replacePlaceholders on both prompt and template before composing
    - buildHtmlPrompt(prompt, template) still works without branding (backward compatible)
  </behavior>
  <action>
    1. Update `src/lib/templates/agent4-html.ts`:
       - Import `replacePlaceholders` from `@/lib/branding`
       - In GENERIC_TEMPLATE: replace `--kb-primary: #6d28d9` with `--kb-primary: {{primaryColor}}`, replace `--kb-accent: #f59e0b` with `--kb-accent: {{accentColor}}`, replace `--kb-secondary: #e5e7eb` with `--kb-secondary: {{secondaryColor}}`, replace `--kb-font: system-ui, -apple-system, sans-serif` with `--kb-font: {{fontFamily}}`
       - In HELPJUICE_PROMPT: replace all `#6d28d9` references with `{{primaryColor}}`
       - In HELPJUICE_TEMPLATE: replace all `#6d28d9` references with `{{primaryColor}}` (there are ~10 occurrences in `var(--accent, #6d28d9)` patterns -- replace with `var(--accent, {{primaryColor}})`)
       - In GENERIC_PROMPT: if any hardcoded color references exist, replace with placeholder references
       - Audit ALL templates (Notion, Confluence) for any `#6d28d9` -- replace if found
       - Update `buildHtmlPrompt` signature to: `buildHtmlPrompt(htmlPrompt: string, htmlTemplate: string, branding?: WorkspaceBranding): string`
       - Inside buildHtmlPrompt, call `replacePlaceholders(htmlPrompt, branding)` and `replacePlaceholders(htmlTemplate, branding)` before composing the final prompt string
       - Import `WorkspaceBranding` from `@/types`

    2. Update `src/lib/pipeline.ts` in `runPhaseB`:
       - Pass `branding` to `buildHtmlPrompt` as third argument: `buildHtmlPrompt(htmlPrompt, htmlTemplate, branding)`
       - Keep the existing prompt-based branding injection (lines 166-173) as a COMPLEMENTARY approach -- both template variables AND prompt text injection work together

    3. Create `src/lib/__tests__/template-neutralization.test.ts`:
       - Test that no DEFAULT_PLATFORM_PROFILES entry contains "#6d28d9" in htmlPrompt or htmlTemplate
       - Test that GENERIC_TEMPLATE contains "{{primaryColor}}"
       - Test that HELPJUICE_TEMPLATE contains "{{primaryColor}}" and not "#6d28d9"
       - Test that buildHtmlPrompt with branding produces resolved colors
       - Test that buildHtmlPrompt without branding still works (backward compat)
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/template-neutralization.test.ts -x && npx vitest run src/lib/__tests__/branding.test.ts -x</automated>
  </verify>
  <done>All 5 built-in templates are free of #6d28d9. Generic and Helpjuice templates use {{placeholder}} variables. buildHtmlPrompt accepts optional branding and resolves placeholders. pipeline.ts passes branding through. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
# Verify no FinBot purple remains in default templates
npx vitest run src/lib/__tests__/template-neutralization.test.ts -x

# Verify branding utility works
npx vitest run src/lib/__tests__/branding.test.ts -x

# Full test suite still passes
npx vitest run
```
</verification>

<success_criteria>
- Zero occurrences of #6d28d9 in any DEFAULT_PLATFORM_PROFILES entry
- replacePlaceholders utility exported and tested
- buildHtmlPrompt accepts optional branding parameter
- pipeline.ts passes branding to buildHtmlPrompt
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-generic-templates-onboarding/08-01-SUMMARY.md`
</output>
