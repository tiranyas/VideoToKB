---
phase: 07-performance-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/stats-migration.sql
  - src/lib/supabase/queries.ts
  - src/app/dashboard/page.tsx
  - src/lib/sse.ts
  - src/lib/__tests__/sse.test.ts
  - src/lib/__tests__/workspace-stats.test.ts
  - src/app/page.tsx
  - src/app/articles/[id]/page.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "Dashboard stats load via a single DB aggregation RPC call, not by fetching all articles client-side"
    - "Recent articles on dashboard come from a separate .limit(5) query, not from the full articles fetch"
    - "SSE parsing logic exists in exactly one file (src/lib/sse.ts) and is imported by both consumers"
    - "Both page.tsx and articles/[id]/page.tsx parse SSE events using the shared readSSEStream utility"
  artifacts:
    - path: "supabase/stats-migration.sql"
      provides: "get_workspace_stats PostgreSQL RPC function"
      contains: "CREATE OR REPLACE FUNCTION get_workspace_stats"
    - path: "src/lib/supabase/queries.ts"
      provides: "getWorkspaceStats wrapper function"
      exports: ["getWorkspaceStats", "WorkspaceStats"]
    - path: "src/lib/sse.ts"
      provides: "Shared SSE stream parser"
      exports: ["readSSEStream"]
    - path: "src/lib/__tests__/sse.test.ts"
      provides: "Unit tests for SSE parser"
    - path: "src/lib/__tests__/workspace-stats.test.ts"
      provides: "Unit tests for workspace stats mapping"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/supabase/queries.ts"
      via: "getWorkspaceStats import"
      pattern: "getWorkspaceStats"
    - from: "src/app/page.tsx"
      to: "src/lib/sse.ts"
      via: "readSSEStream import"
      pattern: "import.*readSSEStream.*from.*@/lib/sse"
    - from: "src/app/articles/[id]/page.tsx"
      to: "src/lib/sse.ts"
      via: "readSSEStream import"
      pattern: "import.*readSSEStream.*from.*@/lib/sse"
---

<objective>
Replace client-side dashboard stats aggregation with a DB-level RPC function, and extract duplicated SSE parsing logic into a shared utility.

Purpose: Dashboard performance scales O(1) instead of O(n) with article count, and SSE parsing has a single source of truth instead of two near-identical implementations.
Output: Migration SQL, updated queries.ts, refactored dashboard, shared SSE utility, updated consumers, unit tests for both.
</objective>

<execution_context>
@C:/Users/tiran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tiran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-and-cleanup/07-RESEARCH.md

@src/lib/supabase/queries.ts
@src/app/dashboard/page.tsx
@src/app/page.tsx
@src/app/articles/[id]/page.tsx
@src/types/index.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/index.ts:
```typescript
export interface ProgressEvent {
  step: PipelineStep | 'error';
  status: StepStatus;
  message?: string;
  article?: string;
  html?: string;
}
```

From src/lib/supabase/queries.ts (existing RPC pattern to follow):
```typescript
export async function getUserUsage(
  supabase: SupabaseClient,
  userId: string
): Promise<UserUsage | null> {
  const { data, error } = await supabase
    .rpc('get_user_usage', { p_user_id: userId });
  // ...
  const row = Array.isArray(data) ? data[0] : data;
  // Number() cast for bigint handling
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB aggregation for dashboard stats (PERF-01)</name>
  <files>supabase/stats-migration.sql, src/lib/supabase/queries.ts, src/app/dashboard/page.tsx, src/lib/__tests__/workspace-stats.test.ts</files>
  <behavior>
    - Test: getWorkspaceStats maps RPC response `{total_articles: 42, this_week: 5, this_month: 12, youtube_count: 20, loom_count: 10, google_drive_count: 8, paste_count: 4}` to `{totalArticles: 42, thisWeek: 5, thisMonth: 12, bySource: {youtube: 20, loom: 10, 'google-drive': 8, paste: 4}}`
    - Test: getWorkspaceStats returns zeroed stats when RPC returns null/empty (workspace not found or no access)
    - Test: getWorkspaceStats throws on RPC error
    - Test: BigInt string values from RPC are correctly converted via Number()
  </behavior>
  <action>
1. Create `supabase/stats-migration.sql` with the `get_workspace_stats` PostgreSQL function. Use `SECURITY DEFINER` with an explicit ownership check: verify `auth.uid()` owns the workspace via the `workspaces` table before running the aggregation query. Return columns: `total_articles`, `this_week`, `this_month`, `youtube_count`, `loom_count`, `google_drive_count`, `paste_count`. Use `COUNT(*) FILTER (WHERE ...)` for conditional counts. Grant EXECUTE to `authenticated` role. Follow the exact SQL from the research file.

2. Create `src/lib/__tests__/workspace-stats.test.ts` with unit tests for the `getWorkspaceStats` function. Mock `supabase.rpc()` to return controlled data. Test the mapping from snake_case DB columns to camelCase TypeScript interface, the null/empty response case, the error case, and BigInt string-to-number conversion.

3. Add to `src/lib/supabase/queries.ts`:
   - Export `WorkspaceStats` interface: `{totalArticles: number, thisWeek: number, thisMonth: number, bySource: {youtube: number, loom: number, 'google-drive': number, paste: number}}`
   - Export `getWorkspaceStats(supabase: SupabaseClient, workspaceId: string): Promise<WorkspaceStats>` function that calls `supabase.rpc('get_workspace_stats', { p_workspace_id: workspaceId })`, handles the array/single response pattern (same as `getUserUsage`), returns zeroed stats if row is null, casts all values with `Number()`.

4. Refactor `src/app/dashboard/page.tsx`:
   - Import `getWorkspaceStats` from `@/lib/supabase/queries`
   - Replace the full articles fetch + client-side aggregation (lines 36-68) with `Promise.all` of two calls: `getWorkspaceStats(supabase, activeWorkspace.id)` and a `.limit(5)` recent articles query
   - Keep the `DashboardStats` interface but remove `totalArticles/thisWeek/thisMonth/bySource` computation — get them from the RPC result
   - Keep `hasCompanyContext` derived from `activeWorkspace` (not from DB)
   - Keep all existing UI rendering unchanged
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/workspace-stats.test.ts -x</automated>
  </verify>
  <done>Dashboard page calls getWorkspaceStats RPC instead of fetching all articles. Stats mapping function has passing unit tests. Migration SQL file exists ready to run in Supabase SQL Editor. Recent articles use .limit(5) query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract SSE parsing into shared utility (PERF-02)</name>
  <files>src/lib/sse.ts, src/lib/__tests__/sse.test.ts, src/app/page.tsx, src/app/articles/[id]/page.tsx</files>
  <behavior>
    - Test: readSSEStream parses a single SSE event `data: {"step":"resolve","status":"complete"}` and calls onEvent with the parsed object
    - Test: readSSEStream handles multiple events separated by double newlines
    - Test: readSSEStream handles chunked data where an event spans two read() calls
    - Test: readSSEStream silently skips malformed JSON lines (does not throw)
    - Test: readSSEStream propagates errors thrown by onEvent callback (does not swallow them)
    - Test: readSSEStream resolves when stream is done
  </behavior>
  <action>
1. Create `src/lib/__tests__/sse.test.ts` with unit tests. Create a helper that builds a mock `Response` object with a readable stream from string chunks. Each test pushes specific SSE-formatted chunks through the mock stream and asserts `onEvent` is called with the correct parsed objects. For the chunked test, split an event across two chunks (e.g., `"data: {\"ste"` and `"p\":\"resolve\",\"status\":\"complete\"}\n\n"`). For the malformed test, include `"data: not-json\n\n"` and verify onEvent is never called. For the propagation test, have onEvent throw and verify readSSEStream rejects with that error.

2. Create `src/lib/sse.ts` exporting `readSSEStream(response: Response, onEvent: (event: ProgressEvent) => void): Promise<void>`. Import `ProgressEvent` from `@/types`. Implementation: get reader from `response.body`, use TextDecoder with `{ stream: true }`, maintain a buffer, split on `\n\n`, parse `data: ` prefixed lines with `JSON.parse`, call `onEvent` for each parsed event, silently skip JSON parse errors (but do NOT catch errors thrown by `onEvent`). This matches the existing implementation in `page.tsx` lines 422-454.

3. Refactor `src/app/page.tsx`:
   - Add `import { readSSEStream } from '@/lib/sse'` at the top
   - Remove the `readSSEStream` function definition at the bottom of the file (lines 422-454)
   - Both `handleSubmit` and `handleGenerateHTML` already call `readSSEStream(response, callback)` — no changes needed to the call sites since the signature is identical

4. Refactor `src/app/articles/[id]/page.tsx`:
   - Add `import { readSSEStream } from '@/lib/sse'`
   - In `handleGenerateHTML` (lines 162-193), replace the inline SSE parsing with:
     ```
     let resultHtml = '';
     await readSSEStream(response, (event) => {
       if (event.step === 'done' && event.html) {
         resultHtml = event.html;
       } else if (event.step === 'error') {
         throw new Error(event.message ?? 'Generation failed');
       }
     });
     ```
   - Remove the `reader`, `decoder`, `buffer` variables and the while loop (lines 162-193)
   - Keep the `if (resultHtml)` block after the readSSEStream call unchanged
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/sse.test.ts -x</automated>
  </verify>
  <done>SSE parsing lives in src/lib/sse.ts. Both page.tsx and articles/[id]/page.tsx import from @/lib/sse. No inline SSE parsing remains in either file. SSE utility has passing unit tests covering normal parsing, chunked data, malformed JSON, and error propagation.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run full test suite: `npx vitest run`
2. Verify no duplicate SSE parsing: `grep -rn "decoder.decode" src/app/ --include="*.tsx"` should return zero results (all SSE decoding now in src/lib/sse.ts)
3. Verify dashboard uses RPC: `grep -n "getWorkspaceStats" src/app/dashboard/page.tsx` should show the import and call
4. Verify no full-table fetch in dashboard: `grep -n "\.select(" src/app/dashboard/page.tsx` should only show the `.limit(5)` recent articles query, not the old full select
5. Build check: `npx next build` completes without errors
</verification>

<success_criteria>
- All vitest tests pass (both sse.test.ts and workspace-stats.test.ts)
- Dashboard page fetches stats via `supabase.rpc('get_workspace_stats')` instead of fetching all articles
- Dashboard recent articles query uses `.limit(5)`
- `src/lib/sse.ts` exports `readSSEStream` and is the only file containing SSE stream parsing logic
- Both `src/app/page.tsx` and `src/app/articles/[id]/page.tsx` import `readSSEStream` from `@/lib/sse`
- `npx next build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-and-cleanup/07-01-SUMMARY.md`
</output>
