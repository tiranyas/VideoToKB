---
phase: 09-more-platforms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/templates/agent4-html.ts
  - src/lib/__tests__/intercom-template.test.ts
  - src/lib/__tests__/zendesk-template.test.ts
  - src/lib/__tests__/all-platforms-registry.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]
must_haves:
  truths:
    - "Intercom prompt explicitly forbids thead, th, tbody, details, summary tags"
    - "Intercom prompt documents strong->b and em->i conversions"
    - "Zendesk template has no branding placeholders"
    - "Intercom template has no branding placeholders"
    - "All 7 platform profiles are registered in DEFAULT_PLATFORM_PROFILES"
    - "Every platform template uses only tags allowed by its platform"
  artifacts:
    - path: "src/lib/templates/agent4-html.ts"
      provides: "Hardened Intercom prompt with NEVER USE list"
      contains: "NEVER USE"
    - path: "src/lib/__tests__/intercom-template.test.ts"
      provides: "Edge case tests for forbidden tags in prompt"
    - path: "src/lib/__tests__/zendesk-template.test.ts"
      provides: "No-branding-placeholder test for Zendesk"
    - path: "src/lib/__tests__/all-platforms-registry.test.ts"
      provides: "Registry test confirming all 7 platforms present"
  key_links:
    - from: "src/lib/templates/agent4-html.ts"
      to: "src/lib/article-generator.ts"
      via: "buildHtmlPrompt called by generateHTML"
      pattern: "buildHtmlPrompt"
---

<objective>
Harden Zendesk and Intercom platform templates with surgical prompt refinements and add edge-case tests that verify platform constraints are enforced.

Purpose: The templates already exist and are largely correct (built in Phase 8, verified in research). This plan closes the gaps identified in 09-RESEARCH.md: (1) Intercom prompt needs an explicit NEVER USE list for tags Intercom silently strips, (2) both templates need verified zero branding placeholders, (3) the full 7-platform registry needs a guard test.

Output: Hardened prompts in agent4-html.ts, expanded test suite covering edge cases.
</objective>

<execution_context>
@C:/Users/tiran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tiran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-more-platforms/09-RESEARCH.md

@src/lib/templates/agent4-html.ts
@src/lib/__tests__/intercom-template.test.ts
@src/lib/__tests__/zendesk-template.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/templates/agent4-html.ts:
```typescript
export function buildHtmlPrompt(
  htmlPrompt: string,
  htmlTemplate: string,
  branding?: WorkspaceBranding
): string;

export const DEFAULT_PLATFORM_PROFILES: PlatformProfile[];
// 7 profiles: generic-html, notion, confluence, helpjuice, zendesk, intercom, markdown-only
```

From src/types/index.ts:
```typescript
interface PlatformProfile {
  id: string;
  name: string;
  htmlPrompt: string;
  htmlTemplate: string;
  isDefault: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Intercom prompt with explicit NEVER USE list and add edge-case tests</name>
  <files>src/lib/templates/agent4-html.ts, src/lib/__tests__/intercom-template.test.ts</files>
  <action>
In src/lib/templates/agent4-html.ts, make these surgical edits to the INTERCOM_PROMPT constant:

1. After the "### Important Restrictions" section (line ~467), add a "### NEVER USE These Tags" section:
```
### NEVER USE These Tags (Intercom silently strips them)
- NO <thead>, <th>, <tbody> — tables must use only <tr> and <td>
- NO <details>, <summary> — Intercom has no accordion/toggle support
- NO <h3>, <h4>, <h5>, <h6> — only h1 and h2 are supported
- NO <div> (except with class="intercom-align-center")
- NO <span>, <section>, <article>, <figure>, <figcaption>
- NO <style>, <script>, <form>, <input>, <textarea>
- NO inline style="" attributes on any element
- NO custom class="" attributes (only intercom-align-center and intercom-h2b-button)
```

2. In the same "### Important Restrictions" section, add this line after the h3-h6 note:
```
- <strong> is converted to <b> and <em> to <i> by Intercom's API — use <b> and <i> directly for consistency
```

3. In the Intercom table component mapping line, reinforce: `(NO thead, NO th — Intercom strips them)`

Do NOT change the INTERCOM_TEMPLATE constant — it already uses the correct tags.
Do NOT change any other platform's prompt or template.

In src/lib/__tests__/intercom-template.test.ts, add these tests:

1. Test "htmlPrompt explicitly warns against thead/th/tbody":
   - Assert `intercom.htmlPrompt` contains "thead" and "th" and "tbody" in a restriction/NEVER context

2. Test "htmlPrompt documents strong-to-b conversion":
   - Assert `intercom.htmlPrompt` contains text about strong being converted to b

3. Test "htmlTemplate has no branding placeholders":
   - Assert `intercom.htmlTemplate` does NOT match /\{\{.*?\}\}/

4. Test "htmlTemplate does NOT contain thead or th":
   - Assert template does not contain `<thead` or `<th`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/intercom-template.test.ts -x</automated>
  </verify>
  <done>Intercom prompt has explicit NEVER USE list with thead/th/tbody/details/summary. Intercom prompt documents strong->b and em->i conversion. All new tests pass. Template unchanged (already correct).</done>
</task>

<task type="auto">
  <name>Task 2: Add Zendesk edge-case tests and all-platforms registry guard</name>
  <files>src/lib/__tests__/zendesk-template.test.ts, src/lib/__tests__/all-platforms-registry.test.ts</files>
  <action>
In src/lib/__tests__/zendesk-template.test.ts, add these tests:

1. Test "htmlTemplate has no branding placeholders":
   - Assert `zendesk.htmlTemplate` does NOT match /\{\{.*?\}\}/

2. Test "htmlTemplate uses thead and tbody for tables":
   - Assert template contains `<thead>` and `<tbody>`

3. Test "htmlPrompt mentions c-callout class variants":
   - Assert prompt contains `c-callout--info` or `c-callout--` with multiple variants (info, warning, tip)

Create new file src/lib/__tests__/all-platforms-registry.test.ts with these tests:

1. Test "all 7 platform profiles are registered":
   - Import DEFAULT_PLATFORM_PROFILES
   - Assert length is 7
   - Assert ids include: generic-html, notion, confluence, helpjuice, zendesk, intercom, markdown-only

2. Test "every profile has non-empty htmlPrompt":
   - Loop all profiles, assert htmlPrompt.length > 0

3. Test "every profile except markdown-only has non-empty htmlTemplate":
   - Loop profiles, for each that is not markdown-only, assert htmlTemplate.length > 0
   - For markdown-only, assert htmlTemplate is empty string

4. Test "no two profiles share the same id":
   - Collect ids into a Set, assert Set.size === array.length

5. Test "platform names match expected display names":
   - Assert each id maps to expected name: generic-html -> "Generic Clean HTML", helpjuice -> "HelpJuice", zendesk -> "Zendesk Help Center", intercom -> "Intercom", notion -> "Notion", confluence -> "Confluence", markdown-only -> "Markdown Only"
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/zendesk-template.test.ts src/lib/__tests__/all-platforms-registry.test.ts -x</automated>
  </verify>
  <done>Zendesk template confirmed to have zero branding placeholders and correct table structure. All 7 platforms verified as registered with correct IDs and names. No duplicate IDs. All profiles have non-empty prompts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Hardened Intercom prompt with explicit NEVER USE tag list and strong->b conversion note.
    Added edge-case tests for Intercom (forbidden tags, no placeholders, conversion docs).
    Added edge-case tests for Zendesk (no placeholders, table structure, callout classes).
    Created all-platforms registry guard test (7 profiles, correct IDs/names, no duplicates).
  </what-built>
  <how-to-verify>
    1. Run `npx vitest run src/lib/__tests__/intercom-template.test.ts src/lib/__tests__/zendesk-template.test.ts src/lib/__tests__/all-platforms-registry.test.ts` — all tests should pass
    2. Open src/lib/templates/agent4-html.ts and search for "NEVER USE" — verify the Intercom section lists thead, th, tbody, details, summary
    3. Verify no other platform prompts or templates were modified (only INTERCOM_PROMPT changed)
    4. Optional: Generate a test article with Intercom platform selected and verify the output contains no forbidden tags
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/intercom-template.test.ts` — all tests pass including new edge cases
2. `npx vitest run src/lib/__tests__/zendesk-template.test.ts` — all tests pass including new edge cases
3. `npx vitest run src/lib/__tests__/all-platforms-registry.test.ts` — all 7 platforms verified
4. `npx vitest run` — full suite green, no regressions
5. Grep for "NEVER USE" in agent4-html.ts — appears in Intercom section
6. Grep for `{{` in Zendesk and Intercom templates — zero matches
</verification>

<success_criteria>
- Intercom prompt has NEVER USE section listing thead, th, tbody, details, summary, div (without allowed class), span, section, style, script
- Intercom prompt documents strong->b and em->i conversions
- Zendesk and Intercom templates confirmed to have zero branding placeholders via tests
- All 7 platform profiles verified by registry guard test (IDs, names, no duplicates)
- Full test suite passes with no regressions
- Only INTERCOM_PROMPT modified in agent4-html.ts — no template changes, no other platform changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-more-platforms/09-01-SUMMARY.md`
</output>
